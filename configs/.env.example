# AgentProp environment template — copy to .env locally; never commit .env

# --- Tier 3: LLM validation (pick one provider path) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# Token-router alternative (used by run_real_routing_case_study.py)
# TOKEN_ROUTER_API_KEY=
# TOKEN_ROUTER_BASE_URL=https://your-router.example/v1
# TOKEN_ROUTER_MODEL=your-model

# GAIA-style benchmark
# GEMINI_API_KEY=

# --- Tier 4: optional Modal GPU for DL sweeps ---
# MODAL_TOKEN_ID=
# MODAL_TOKEN_SECRET=
